[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
[user]
	name = Bei Yang
	email = bei.yang123@gmail.com
[alias]
	st = status
	co = checkout
	br = branch
	ci = commit
	graph = log --color --graph --pretty=format:'%Cred%h%Creset - %s - %C(yellow)%d%Creset - %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

[push]
	default = tracking
[color]
    ui = auto
    branch = auto
    status = auto
    diff = auto
[core]
	filemode = false
	editor = /usr/bin/vim	
